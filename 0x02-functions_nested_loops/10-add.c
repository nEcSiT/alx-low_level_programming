#include "main.h"

/**
 * add - a function that add two integers
 * @a: carries the first int
 * @b: carries the second int
 * Return: 0
 */

int add(int a, int b)
{
	a = 2;
	b = 3;


	_putchar(add(a, b));
	_putchar('\n');

return (a + b);
}
