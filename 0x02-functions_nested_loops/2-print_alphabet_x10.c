#include "main.h"

/**
 *print_alphabet_x10 - Prints 10x the alphabets
 */

void print_alphabet_x10(void)

{
	int nic = 0;
	int dor;

		while (nic <= 9)
		{
			dor = 'a';
			while (dor <= 'z')
				_putchar(dor);
					dor++;
			{

			}
			_putchar('\n');
				nic++;
		}

}
