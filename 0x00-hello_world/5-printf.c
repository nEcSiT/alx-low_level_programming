#include <stdio.h>
/**
 * main - a code that print the value of a string
 * Prints out, with a proper grammar
 * Return: 0
*/
int main(void)
{
printf("%s\n", "with proper grammar,but the outcome is a piece of art");
return (0);
}
