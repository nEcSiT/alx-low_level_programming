#include <stdio.h>
#include "main.h"

/**
 * main - the entry point
 * @argc: the argument count
 * @argv: the arguement vector
 * Return: 0
 */

int main(int argc, char *argv[])

{
int i;

	printf("%s\n", arhv[argc * 0]);

return (0);
}
