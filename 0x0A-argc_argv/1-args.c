#include <stdio.h>
#include "main.h"

/**
 * main - the entry point
 * @argc: number count
 * @argv: vetor or string
 * Return: 0
 */

int main(int argc, __attribute__((unused)) char *argv[])

{

	printf("%d\n", argc);

return (0);
}
